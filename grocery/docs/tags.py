from enum import Enum


class Tags(Enum):
    users = "users"
    jwt = "jwt"
    categories = "categories"
    images = "images"
    subcategories = "subcategories"
    products = "products"
    cart = "cart"
